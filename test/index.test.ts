// import { zeros, NestedArray } from 'zarr';
// import { ZarrLoader } from '../src/zarrLoader';

test('test image pyramid creation', () => {
  expect(2).toEqual(2);
});

test('image creation', () => {
  expect(1).toEqual(1);
});
